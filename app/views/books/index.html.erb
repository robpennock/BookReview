<% @books.each do |book| %>
	<% #show all books and link to specific book via passed path %>
	<h2><%= link_to book.title, book_path(book) %>, <%= book.author %> </h2>
<% end %>

<%= link_to "Add a Book", new_book_path %>
